class TransferPointsInteractor {
  Future<void> loadSimulation() => Future.delayed(
        const Duration(seconds: 2),
//        () => throw 'Error',
      );

  Future<void> loadSummary(int value) => Future.delayed(
        const Duration(seconds: 2),
//        () => throw 'Error',
      );

  Future<void> loadResult() => Future.delayed(
        const Duration(seconds: 2),
//        () => throw 'Error',
      );
}
